---
description: Build the release .app bundle and deploy to Applications
---

To build and deploy MacWiggleBox:

// turbo
1. Run `./scripts/package.sh`
// turbo
2. Run `mv MacWiggleBox.app /Applications/MacWiggleBox.app`
